# Temas Kafka - Fase I Ingesta (Broker: 192.168.99.10:9092)
# Crear con: kafka-topics.sh --bootstrap-server 192.168.99.10:9092 --create ...

# Datos crudos (desde NiFi / APIs y logs GPS)
topic.raw=raw-data
datos.crudos.replicas=1
datos.crudos.partitions=3

# Datos filtrados (post-procesamiento NiFi)
topic.filtered=filtered-data
datos.filtrados.replicas=1
datos.filtrados.partitions=3
